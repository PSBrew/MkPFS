# Local .venv Activation

Activate `.venv` before running any project command:

```bash
source .venv/bin/activate
```

Without it, `run-tests.sh`, CLI invocations, and scripts will fail or use wrong Python/dependencies.
